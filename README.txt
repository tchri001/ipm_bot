Bot to play Idle Planet Miner on BlueStacks Android Emulator.
Written in Python 3.12.1

This is my first attempt building full programs in Python.

bot_program startup alignment note:
- Startup anchor alignment currently assumes BlueStacks is opened in the ad-banner layout.
- Use the same window/layout each run for consistent startup positioning.

pip install -r requirements.txt